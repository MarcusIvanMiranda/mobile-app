import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent will call AppRegistry.registerComponent for you
// and ensure the app content is rendered in the right environment.
registerRootComponent(App);